#include <bits/stdc++.h>
#define FAST                          \
    ios_base::sync_with_stdio(false); \
    cin.tie();                        \
    cout.tie();
using namespace std;
struct Transaction
{
    int payer, receiver, money;
};

vector<Transaction> solve(vector<Transaction> &transcations)
{
}
